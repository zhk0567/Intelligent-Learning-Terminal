<?xml version="1.0" encoding="utf-8"?>
<!-- 刷新图标：圆弧 + 箭头，线性简笔画风格 -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="18dp"
    android:height="18dp"
    android:viewportWidth="24"
    android:viewportHeight="24">

    <!-- 圆弧（约 300° 的圆，留缺口给箭头） -->
    <path
        android:pathData="M20,12 A8,8 0 1,1 14.5,4.5"
        android:strokeColor="@color/icon_tertiary"
        android:strokeWidth="1.8"
        android:strokeLineCap="round"
        android:fillColor="@android:color/transparent"/>

    <!-- 箭头头部（三角形，朝右上） -->
    <path
        android:pathData="M14,2 L17,5.5 L13,6.5 Z"
        android:fillColor="@color/icon_tertiary"
        android:strokeColor="@android:color/transparent"/>

</vector>
