# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Daemon 堆：过小会频繁 GC，拖慢配置与编译；可按机器内存改为 6144m
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8 -XX:+UseParallelGC -XX:MaxMetaspaceSize=512m

# 多模块并行配置与执行
org.gradle.parallel=true
# 跨构建复用任务输出（干净构建后增量编译受益大）
org.gradle.caching=true
# 复用配置阶段，减少每次「配置 Gradle 工程」耗时；若插件不兼容可改为 problems=warn 或关闭
org.gradle.configuration-cache=true
# org.gradle.configuration-cache.problems=warn

# 各模块 R 类仅含本模块资源，减少 javac/kotlin 无效重编译
android.nonTransitiveRClass=true

kotlin.code.style=official
kotlin.daemon.jvmargs=-Xmx2048m

# 静态资源根（勿尾斜杠），与 Web `VITE_STATIC_ORIGIN`、小程序 `STATIC_ORIGIN` 一致。
# 无域名时可用 http://公网IP；留空则「基础学习」视频仍用 BasicLessons.LESSON_VIDEO_BASE（模拟器连本机 Vite）。
# STATIC_ASSET_ORIGIN=http://你的公网IP
# STATIC_ASSET_ORIGIN=https://assets.example.com